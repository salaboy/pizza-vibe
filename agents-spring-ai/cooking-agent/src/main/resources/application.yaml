server:
  port: 8087

spring:
  ai:
    anthropic:
      api-key: ${ANTHROPIC_API_KEY:test-key}
      chat:
        options:
          model: claude-haiku-4-5-20251001
    mcp:
      client:
        sse:
          connections:
            pizza-mcp:
              url: ${PIZZA_MCP_URL:http://localhost:8086}

store:
  service:
    url: ${STORE_SERVICE_URL:http://localhost:8080}
